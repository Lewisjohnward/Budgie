// import app from "../index";
// import request from "supertest";


describe("app", () => {
  it.todo("placeholder")
})

// describe("GET /", () => {
//   it.todo(
//     'responds with "Welcome to unit testing guide for nodejs, typescript and express!',
//     async () => {
//       const response = await request(app).get("/");
//       expect(response.status).toBe(200);
//       expect(response.text).toBe("hello from /");
//     },
//   );
// });
