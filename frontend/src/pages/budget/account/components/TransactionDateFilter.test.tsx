import { describe, it, expect } from "vitest";
import { TransactionDateFilter } from "./TransactionDateFilter";

describe("Account - TransactionDateFilter", () => {
  it("should work", () => {
    expect(true).toBe(true);
  });
  it.skip("should display the current month in from when there are no transactions", async () => {
    expect.hasAssertions();
  });
  it.skip("should display the current year in from when there are no transactions", async () => {
    expect.hasAssertions();
  });
  it.skip("should display the year + 10 in to when there are no transactions", async () => {
    expect.hasAssertions();
  });
  it.skip("should display the currentmonth in to when there are no transactions", async () => {
    expect.hasAssertions();
  });
});
